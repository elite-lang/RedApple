List链表这个结构，用代码构建并不是十分容易

于是有了这样的约定，防止出现问题：

1. 每个BNF范式构建的链表都是以第一个元素开头，没有头节点，也没有父节点
2. 每个BNF范式在孩子的添加时，使用make_list函数和getList函数，这两个函数都会检查当前链表的长度，如果只有一个元素，则将其挂在对应位置，否则则添加一个Node节点，作为这个链的父亲。
3. 单纯想将一个链表接到一个链表的后面，需要使用addBrother函数
